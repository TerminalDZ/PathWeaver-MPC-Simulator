{
  "name": "dash",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && webpack",
    "watch": "set NODE_OPTIONS=--openssl-legacy-provider && webpack --watch"
  },
  "author": "Matt Bradley",
  "license": "MIT",
  "devDependencies": {
    "script-loader": "^0.7.2",
    "webpack": "^5.99.8",
    "webpack-cli": "^6.0.1",
    "wrapper-webpack-plugin": "1.0.0"
  },
  "dependencies": {
    "npm": "^5.7.1"
  }
}
